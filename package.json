{
  "name": "hr-candidate-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev --turbopack",
    "build": "prisma generate && next build",
    "build:local": "NODE_ENV=development prisma generate && next build",
    "build:dev": "NODE_ENV=development prisma generate && next build",
    "build:test": "NODE_ENV=test prisma generate && next build",
    "build:production": "NODE_ENV=production prisma generate && next build",
    "start": "next start",
    "start:local": "NODE_ENV=development next start",
    "start:dev": "NODE_ENV=development next start",
    "start:test": "NODE_ENV=test next start",
    "start:production": "NODE_ENV=production next start",
    "lint": "next lint",
    "postinstall": "prisma generate",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate deploy"
  },
  "dependencies": {
    "@prisma/client": "^6.12.0",
    "crypto-js": "^4.2.0",
    "lucide-react": "^0.525.0",
    "next": "15.4.3",
    "playwright": "^1.54.2",
    "prisma": "^6.12.0",
    "react": "19.1.0",
    "react-dom": "19.1.0"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3",
    "@tailwindcss/postcss": "^4",
    "@types/crypto-js": "^4.2.2",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "15.4.3",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}
